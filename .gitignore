# private files
private

# environment variables
.env

# ansible hosts file
deploy/hosts.yml

# GitHub read-only deploy key (written from GITHUB_REPO_DEPLOY_KEY in CI)
deploy/files/github_deploy_key

# compose override file
compose.override.yml
!deploy/**/compose.override.yml
