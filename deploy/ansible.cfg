[defaults]
inventory = ./hosts.yml
remote_user = ubuntu
host_key_checking = False
strategy = free

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false

[ssh_connection]
pipelining = True
